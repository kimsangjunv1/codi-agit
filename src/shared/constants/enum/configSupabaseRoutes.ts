export enum SupabaseRoute {
  // invitation 관련
  // CONFIG_INVITATION_KEY = "invitation",
  // CONFIG_INVITATION_GET = "/invitation?select=*",
  // CONFIG_INVITATION_POST = "/invitation",

  // todos 관련
  TODO_KEY = "todos",
  TODO_GET = "/todos?select=*",
  TODO_POST = "/todos",

  // users 관련
  USER_KEY = "users",
  USER_GET = "/users?select=*",
  USER_POST = "/users",
}